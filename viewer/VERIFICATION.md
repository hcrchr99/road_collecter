# 第一版验证记录 · 2026-09-25

## 已完成

命令 `node tests/viewer.test.cjs`：9 组测试通过，含 CSV 引号/BOM/换行，STORE ZIP，v0.6～v0.9 缺字段包，压缩/截断/缺必需文件拒绝，事件 ID、十类标签词表、跨包关联与 JSON 契约检查。

大包为 **69.6 MiB（约 73 MB）合成 ZIP**，含 120,000 条样本及 3,148 个帧条目。Node 数据层解析约 0.6 秒，浏览器页面记录端到端解析 **252 ms**。图片为占位字节，这一结果仅验证加载和解析吞吐，不能替代 71.6 MB 真实包验收。

浏览器实际操作验证：

- 初始合成演示正常，12 个事件、3 段、3 条人工判读示例。
- 点选事件 #11 联动照片并显示「过暗」，前一帧切到冲击前 400 ms。
- 低速筛选保留事件 #3。
- 文件选择器导入 STORE ZIP，统计与测试输入一致；损坏 JPEG 显示错误占位。
- 新包清除前一个包的 AI 示例。分别导入判读与报告 JSON，卡片及报告正常渲染。
- 导入 method=8 ZIP 明确报错，原已载入状态保留。
- 桌面、390 px 手机宽度检查；无页面级横向溢出，宽事件表在局部容器滚动。
- 浏览器错误日志为空；`git diff --check` 通过。

## 尚未完成的外部验收

- 指定 v0.8 真包（388 事件 / 10.7 km / 36.3 个/km / 87.9%）与 v0.9 首包不在当前仓库，未验证。
- 画面可用率当前使用 `1 - frame_quality.suspect_ratio`，须与实包验收报告核对是否同一口径。
- 高德 Key / 安全代理尚未提供。在线底图、坐标转换与路段叠加代码已接入，未以真实服务验证；默认展示明确标记的离线相对轨迹图。
- 未调用真实 LLM；两个区域为支持 JSON 接入的界面，内置内容均是 human_example。
- 未在实体 iPhone / Safari 测试；手机检查是浏览器视口测试。
- 未提交、推送或部署线上；本地预览不是公网发布。
