# 项目状态快照

> **这个文件要随每次实质进展更新。** 新加入的人或 AI Agent 读这一个文件就能上手，
> 不用翻聊天记录。约定见 `docs/协作与数据共享规范.md` 第 7 节。

更新于：2026-09-18 23:45
更新人：（待填）

---

## 阶段

**真实数据采集 + 标注阶段。** 算法链路已完整跑通并有量化报告背书，现在缺的是真实数据。

## 硬约束

- **截止：2026-09-30**（iCAN 大赛，赛项 id=138，软件赛道）
- 交付三件套：20 页内 PDF 方案 + 5 分钟 MP4 演示 + 可运行链接/源码
- 评分构成：创新性 30 / 技术实现 30 / 实用价值 20 / 用户体验 10 / 展示效果 10

## 数据资产

| 类型 | 数量 | 位置 | 状态 |
|---|---|---|---|
| 真实采集包 | 1 份 | 私有盘 `raw/` | 已登记，待标注 |
| 黄金集 | 0 份 | — | 未建 |
| 合成演示包 | 1 份 | `examples/demo_output/` | 已入库 |

采集包明细见 `labels/MANIFEST.csv`。

## 当前代码状态

- `analyze.py` / `vision.py` / `fusion.py` / `tune_detector.py` / `replay_detect.py` 均可独立自检
- `reports/` 下 7 份报告已重新生成，与实际代码一致
- Vercel 零构建站点配置就绪，**尚未真正 Import 部署**
- 协作规范与工具已就位（`tools/`）

## 最近的结论

1. **双模态正交性是核心创新点。** IMU 管深度/结构、视觉管类型/形态，失效模式互不重叠，
   融合后病害覆盖 4/7 → 7/7。这是「创新性 30 分」最该吃住的地方。
2. **速度门控问题是规则问题，不是程序 bug。** 真实采集包全部速度读数为 0，
   导致事件静默全灭。已产出 v0.3（硬门控改软降权）。
3. **报告乱码是 stdout 编码问题，已从根上修掉。** Windows 下 `print` 重定向默认 GBK，
   已在 5 个脚本入口强制 UTF-8。

## 下一步（按优先级）

1. **实采 3-5 趟真实骑行数据**（不同路面），跑通 `analyze → vision → fusion` 全链路
   —— 这是一切的前提，后面的量化对比都依赖它
2. 量化经典 CV 基线在真实帧上的退化程度（合成图 1.000，真实数据会明显更低）
3. 建 100 帧纯人工黄金集（只评估不训练）
4. 补齐展示端：腾讯位置服务底图 + 融合 GeoJSON 叠加（GCJ-02）
5. Vercel 正式部署 + 手机端按 `DEPLOY.md` 自检
6. 凑三件套

## 已知待办

- [ ] 文档版本号 `视觉融合方案_v0.2.html` 与实际 v0.3 不符
- [ ] 首页缺 `docs/` 入口链接
- [ ] `.vercel/project.json` 空字段不该入库
- [ ] Vercel 尚未真正部署

## 阻塞

无。

## 给接手者的提示

- **先读**：`README.md` → `docs/协作与数据共享规范.md` → 本文件
- **动手前**：跑一遍 `python analyze.py --selftest`（等 5 个脚本的自检），确认环境正常
- **要真实数据**：找采集包登记表 `labels/MANIFEST.csv` 看有哪些，再到私有盘取
- **改代码后**：如果改动影响输出数字，记得重跑 `reports/` 下对应报告
- **提交前**：跑 `python tools/merge_labels.py --mode guard`
