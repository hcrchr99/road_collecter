# 项目状态快照

## 2026-09-25 展示端增量（本地完成，待真实数据验收）

- 新增 `viewer/index.html`：单文件、无构建，五个区域完整实现：导入概览、轨迹/事件/帧回看、视觉判读 JSON、路段报告 JSON、局限说明。
- 采集端新增观察台链接。可从 `/viewer/index.html` 访问；尚未 commit/push 或发布。
- ZIP 使用后台 Worker 解析、帧按需解码；v0.6–v0.9 缺字段显示 N/A，压缩 ZIP 明确拒绝。低速事件保留，算法标签与视觉结论独立展示。
- 内置全部为合成路线/程序绘图与 human_example 文案，不作为真实采集、人工真值或模型准确率证据。
- 高德 JS API 接入口已实现，默认离线相对轨迹图。在线地图仍需团队 Key/安全配置，尚未进行真实服务验收。
- 测试入口 `node tests/viewer.test.cjs`。测试合成包写入已忽略、不会部署的 `userdata/viewer-test/`。
- 尚缺交接清单指定的 v0.8/v0.9 真实采集包，以及核对过的真实事件示例文案；未声称已达到 388 事件 / 10.7 km / 87.9% 的实包验收。
- 以下原有快照保留作历史记录，其版本、资产数量及部署状态可能已过时。

---

> **这个文件要随每次实质进展更新。** 新加入的人或 AI Agent 读这一个文件就能上手，
> 不用翻聊天记录。约定见 `docs/协作与数据共享规范.md` 第 7 节。

更新于：2026-09-19 00:40
更新人：（待填）

---

## 阶段

**真实数据采集 + 标注阶段。** 算法链路已完整跑通并有量化报告背书。
采集端与标注工具已补齐，现在缺的是真实数据本身。

## 硬约束

- **截止：2026-09-30**（iCAN 大赛，赛项 id=138，软件赛道）
- 交付三件套：20 页内 PDF 方案 + 5 分钟 MP4 演示 + 可运行链接/源码
- 评分构成：创新性 30 / 技术实现 30 / 实用价值 20 / 用户体验 10 / 展示效果 10

## 数据资产

| 类型 | 数量 | 位置 | 状态 |
|---|---|---|---|
| 真实采集包 | 1 份 | 项目资产 `road_check/raw/` | 已登记，待标注 |
| 标注数据 | 0 份 | 项目资产 `road_check/label/` | 未开始 |
| 黄金集 | 0 份 | 项目资产 `road_check/golden/` | 未建 |
| 合成演示包 | 1 份 | `examples/demo_output/` | 已入库 |

采集包明细见 `labels/MANIFEST.csv`。

## 当前代码状态

- `analyze.py` / `vision.py` / `fusion.py` / `tune_detector.py` / `replay_detect.py` 均可独立自检
- `reports/` 下 7 份报告已重新生成，与实际代码一致
- `index.html` 采集端已补**采集者信息强制填写**（不填无法开始采集）
- `tools/label_tool.html` 图形化标注工具已就位并通过端到端测试
- Vercel 零构建站点配置就绪，**尚未真正 Import 部署**

## 最近的结论

1. **双模态正交性是核心创新点。** IMU 管深度/结构、视觉管类型/形态，失效模式互不重叠，
   融合后病害覆盖 4/7 → 7/7。这是「创新性 30 分」最该吃住的地方。
2. **速度门控问题是规则问题，不是程序 bug。** 真实采集包全部速度读数为 0，
   导致事件静默全灭。已产出 v0.3（硬门控改软降权）。
3. **报告乱码是 stdout 编码问题，已从根上修掉。** Windows 下 `print` 重定向默认 GBK，
   已在 5 个脚本入口强制 UTF-8。
4. **网页端「自动识别」不是视觉识别，是 4 行阈值规则。** `index.html` 的 `autoType()`
   只看事件时长与峰值，摄像头完全不参与判定。其中 `return '井盖'` 是兜底分支，
   会把「又短又不够尖」的事件全归为井盖 —— 这是 Kappa 分析中「井盖↔减速带」
   分歧最集中的根源。真正的视觉识别在 `vision.py`，与网页端是两条路。
5. **采集者信息必须随包落盘，不能事后登记。** 它不可再生，且报告不是事实源。

## 下一步（按优先级）

1. **实采 3-5 趟真实骑行数据**（不同路面），跑通 `analyze → vision → fusion` 全链路
   —— 这是一切的前提，后面的量化对比都依赖它
2. 用 `tools/label_tool.html` 对采集包做标注，产出第一批 `labels.csv` / `frames.csv`
3. 量化经典 CV 基线在真实帧上的退化程度（合成图 1.000，真实数据会明显更低）
4. 建 100 帧纯人工黄金集（只评估不训练）
5. 补齐展示端：腾讯位置服务底图 + 融合 GeoJSON 叠加（GCJ-02）
6. Vercel 正式部署 + 手机端按 `DEPLOY.md` 自检
7. 凑三件套

## 已知待办

- [ ] 文档版本号 `视觉融合方案_v0.2.html` 与实际 v0.3 不符
- [ ] 首页缺 `docs/` 入口链接
- [ ] `.vercel/project.json` 空字段不该入库
- [ ] Vercel 尚未真正部署
- [ ] `autoType()` 的兜底分支「井盖」建议改成显式「未定」并提示人工确认
- [ ] 采集端缩略图 480×360 / 质量 0.6 偏小（长边已可用标注工具在大屏看，
      但采集包内帧本身分辨率低，考虑提到 720×540，需权衡包体积）

## 阻塞

无。

## 给接手者的提示

- **先读**：`README.md` → `docs/协作与数据共享规范.md` → 本文件
- **动手前**：跑一遍 `python analyze.py --selftest`（等 5 个脚本的自检），确认环境正常
- **要真实数据**：找采集包登记表 `labels/MANIFEST.csv` 看有哪些，再到项目资产 `road_check/raw/` 取
- **要标注**：浏览器打开 `tools/label_tool.html`，拖入 zip 即可，无需环境
- **改代码后**：如果改动影响输出数字，记得重跑 `reports/` 下对应报告
- **提交前**：跑 `python tools/merge_labels.py --mode guard`
